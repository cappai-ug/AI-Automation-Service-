# Puppeteer is only used for the local PDF-generation script (npm run pdf).
# Skip the Chromium download by default so it doesn't run on every Vercel build.
# Run `npm run pdf:setup` once locally to install Chromium.
puppeteer-skip-download=true
